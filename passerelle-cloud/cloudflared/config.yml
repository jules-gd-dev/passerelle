tunnel: ${TUNNEL_UUID:?TUNNEL_UUID must be set}
credentials-file: /etc/cloudflared/${TUNNEL_UUID}.json

ingress:
  - hostname: ${TUNNEL_HOSTNAME:?TUNNEL_HOSTNAME must be set}
    service: http://web:5173
  - service: http_status:404
